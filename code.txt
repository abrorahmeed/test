<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Summage - Landing Page</title>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: linear-gradient(135deg, #4A90E2, #357ABD);
      color: #333;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    /* Container */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 1rem;
    }

    /* Navbar */
    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 0;
    }

    nav .logo {
      font-size: 1.5rem;
      font-weight: 700;
      color: #fff;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      align-items: center;
    }

    nav ul li {
      font-weight: 500;
      color: #fff;
    }

    nav .btn {
      background: #4CAF50;
      padding: 0.5rem 1.2rem;
      border-radius: 25px;
      color: #fff;
      font-weight: 600;
      transition: 0.3s;
    }

    nav .btn:hover {
      background: #43a047;
    }

    /* Hero */
    .hero {
      background: #fff;
      border-radius: 30px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 3rem 2rem;
      margin-top: 2rem;
      gap: 2rem;
      flex-wrap: wrap;
    }

    .hero-text {
      flex: 1;
      min-width: 280px;
    }

    .hero-text h4 {
      color: #4A90E2;
      margin-bottom: 0.5rem;
      font-size: 1rem;
      font-weight: 600;
    }

    .hero-text h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      line-height: 1.2;
    }

    .hero-text h1 span {
      color: #4A90E2;
    }

    .hero-text .cta-btn {
      display: inline-block;
      background: #f9a825;
      color: #fff;
      padding: 0.8rem 2rem;
      border-radius: 30px;
      font-size: 1rem;
      font-weight: 600;
      transition: 0.3s;
    }

    .hero-text .cta-btn:hover {
      background: #f57f17;
    }

    .hero-img {
      flex: 1;
      min-width: 280px;
      text-align: center;
    }

    .hero-img img {
      max-width: 100%;
      height: auto;
      animation: float 4s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    /* Responsive */
    @media (max-width: 1024px) {
      .hero {
        flex-direction: column;
        text-align: center;
      }

      .hero-text {
        margin-bottom: 2rem;
      }

      nav ul {
        gap: 1rem;
      }
    }

    @media (max-width: 600px) {
      nav {
        flex-direction: column;
        gap: 1rem;
      }

      nav ul {
        flex-direction: column;
      }

      .hero-text h1 {
        font-size: 2rem;
      }

      .hero-text .cta-btn {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Navbar -->
    <nav>
      <div class="logo">Summage</div>
      <ul>
        <li><a href="#">Home</a></li>
        <li><a href="#">Courses</a></li>
        <li><a href="#">About</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#" class="btn">Begin</a></li>
      </ul>
    </nav>

    <!-- Hero -->
    <section class="hero">
      <div class="hero-text">
        <h4>Online Education</h4>
        <h1>Start learning <span>at home</span></h1>
        <a href="#" class="cta-btn">Begin Learning</a>
      </div>
      <div class="hero-img">
        <!-- Replace with your SVG/PNG illustration -->
        <img src="https://i.ibb.co/8br6LJG/education-illustration.png" alt="Education Illustration">
      </div>
    </section>
  </div>
</body>
</html>
